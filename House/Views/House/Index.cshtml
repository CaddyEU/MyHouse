@model IEnumerable<House.Models.HouseListViewModel>

@{
    int rowNumber = 0;
}

<div class="h1">
    Houses
</div>

<div>
    <a asp-action="add" asp-controller="House" type="submit" class="btn btn-primary">Add</a>
</div>

<form method="post" class="m-1">
    <table class="table table-hover">
        <thead>
            <tr>
                <th scope="col">Nr</th>
                <th scope="col">OwnerName</th>
                <th scope="col">Address</th>
                <th scope="col">SquareMeters</th>
                <th scope="col">YearOfBuild</th>
                <th scope="col">NumberOfRooms</th>
                <th scope="col">NumberOfFloors</th>
                <th scope="col">Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var house in Model)
            {

                rowNumber += 1;
                <tr>
                    <th scope="row">@rowNumber</th>
                    <td class="">@house.OwnerName</td>
                    <td class="">@house.Address</td>
                    <td class="">@house.SquareMeters</td>
                    <td class="">@house.YearOfBuild</td>
                    <td class="">@house.NumberOfRooms</td>
                    <td class="">@house.NumberOfFloors</td>
                    <td class="">@house.Price</td>
                    <td class="">
                        <a asp-action="edit" asp-controller="house" asp-route-id="@house.Id" class="btn btn-primary">Update</a>
                        <a asp-action="delete" asp-controller="house" asp-route-id="@house.Id" type="submit" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</form>
